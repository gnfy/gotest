/* package main

import (
	"fmt"
	"gotest/user"
)

func main() {
	// 短变量申明
	s := user.Hello()
	fmt.Printf("s: %v\n", s)
	// 传统的变量申明
	var (
		name  string
		age   int
		email string
	)

	// 赋值
	name, age = getNameAge()
	email = "a@a.cn"
	// 打印
	fmt.Printf("name: %v, age: %v, email: %v", name, age, email)
}

func getNameAge() (string, int) {
	return "abc", 20
}
*/